import { createServiceSupabaseClient } from "@/lib/supabase";
import { leadSchema } from "@/lib/validation";
const MAX_BODY_BYTES=16*1024;
export async function POST(request:Request){const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)return Response.json({error:"Origem não permitida."},{status:403});const contentLength=Number(request.headers.get("content-length")??0);if(Number.isFinite(contentLength)&&contentLength>MAX_BODY_BYTES)return Response.json({error:"Dados enviados excedem o limite permitido."},{status:413});let body:unknown;try{body=await request.json()}catch{return Response.json({error:"Dados inválidos."},{status:400})}const parsed=leadSchema.safeParse(body);if(!parsed.success)return Response.json({error:"Revise os campos informados."},{status:400});const value=parsed.data;if(value.website||Date.now()-value.formStartedAt<2500)return Response.json({ok:true},{status:201});const client=createServiceSupabaseClient();if(!client)return Response.json({error:"O recebimento de contatos ainda não foi configurado. Tente novamente mais tarde."},{status:503});const{error}=await client.from("leads").insert({name:value.name,phone:value.phone,email:value.email||null,establishment:value.establishment,city:value.city||null,ambassador_id:value.ambassadorId,source_page:value.sourcePage,monthly_revenue:value.monthlyRevenue,contact_preference:value.contactPreference,consent_lgpd:true,utm_source:value.utmSource||null,utm_medium:value.utmMedium||null,utm_campaign:value.utmCampaign||null,utm_content:value.utmContent||null,utm_term:value.utmTerm||null});if(error)return Response.json({error:"Não foi possível registrar seu contato agora."},{status:500});return Response.json({ok:true},{status:201})}
